apiVersion: batch/v1
kind: CronJob
metadata:
  name: reservation-all-motels
  namespace: api-traffic
spec:
  schedule: "* * * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: trafficgen
              image: 520320208231.dkr.ecr.us-west-2.amazonaws.com/api-traffic-generator:v1.0.0
              imagePullPolicy: Always
              env:
                - name: TASK
                  value: "reservation_all_motels"
                - name: BASE_URL
                  valueFrom:
                    configMapKeyRef:
                      name: trafficgen-config-reservation
                      key: BASE_URL
                - name: START_PAGE
                  value: "1"
                - name: RESV_PER_PAGE
                  value: "50"
                - name: RESV_PAGE_PARAM
                  value: "page"
                - name: RESV_PER_PAGE_PARAM
                  value: "per_page"
              resources:
                requests: { cpu: "50m", memory: "64Mi" }
                limits:   { cpu: "250m", memory: "256Mi" }