apiVersion: batch/v1
kind: CronJob
metadata:
  name: get-motels
  namespace: api-traffic
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: trafficgen
              image: 520320208231.dkr.ecr.us-west-2.amazonaws.com/api-traffic-generator:v1.0.0
              imagePullPolicy: IfNotPresent
              env:
                - name: TASK
                  value: "get_motels"
                - name: BASE_URL
                  valueFrom:
                    configMapKeyRef:
                      name: trafficgen-config-motel
                      key: BASE_URL
                - name: PAGE_SIZE
                  value: "50"
                - name: CHAIN_LOOKUP
                  value: "true"
              resources:
                requests: { cpu: "50m", memory: "64Mi" }
                limits:   { cpu: "250m", memory: "256Mi" }